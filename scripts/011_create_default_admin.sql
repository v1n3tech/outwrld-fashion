-- Removed this script as it conflicts with the main trigger function
-- The admin role assignment is now handled in the main handle_new_user() function
-- Simply sign up with email 'mantimdanzaki@gmail.com' and password 'password' 
-- to automatically get admin privileges

-- This file is kept for reference but the functionality is moved to 008_create_triggers_functions.sql
SELECT 'Admin user setup moved to handle_new_user() function in 008_create_triggers_functions.sql' as message;
